#include "validationobjective.h"

#include <QApplication>

int main(int argc, char *argv[])
{
    QApplication a(argc, argv);
    ValidationObjective w;
    w.show();
    return a.exec();
}
